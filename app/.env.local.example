# ================================
# Lux Faucet - Next.js Environment Variables
# ================================
# Copy this file to .env.local and fill in your values
# Documentation: https://nextjs.org/docs/app/building-your-application/configuring/environment-variables

# ================================
# API Configuration
# ================================

# Backend API endpoint
# Default: http://localhost:8000 (development)
# Production: https://api.faucet.lux.network or your deployed backend URL
# Note: Remove /api suffix if your backend doesn't use it
NEXT_PUBLIC_API_URL=http://localhost:8000

# ================================
# Google ReCAPTCHA v3
# ================================

# ReCAPTCHA v3 Site Key (public key - safe to expose in frontend)
# Get your keys from: https://www.google.com/recaptcha/admin
# 1. Create a new site with reCAPTCHA v3
# 2. Add your domains (localhost for dev, your domain for prod)
# 3. Copy the Site Key here
# Note: The Secret Key goes in the backend .env file, NOT here
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# ================================
# WalletConnect (Optional)
# ================================

# WalletConnect Cloud Project ID
# Required for WalletConnect v2 integration
# Get your Project ID from: https://cloud.walletconnect.com
# 1. Sign in/up at WalletConnect Cloud
# 2. Create a new project
# 3. Copy the Project ID
# Note: Optional - the app will work without it, but WalletConnect won't be available
NEXT_PUBLIC_WC_PROJECT_ID=your_walletconnect_project_id_here

# ================================
# Additional Configuration (Optional)
# ================================

# Chain ID (if you want to override the default)
# Lux Testnet: 8888
# Lux Mainnet: 9999 (not for faucet)
# NEXT_PUBLIC_DEFAULT_CHAIN_ID=8888

# Custom RPC URL (if not using the default)
# NEXT_PUBLIC_RPC_URL=https://testnet.lux.network

# Google Analytics (optional)
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# ================================
# Notes
# ================================
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Never put secrets or private keys in NEXT_PUBLIC_* variables
# - Backend secrets (private keys, API keys) go in the backend .env file
# - For production, use environment variables from your hosting provider
#   (Vercel, Netlify, etc.) instead of .env files
